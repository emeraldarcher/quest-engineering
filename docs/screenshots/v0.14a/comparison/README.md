# v0.13 → v0.14a town comparison

Both PNGs are raw Pixi town canvases at 1440×900.

- `before-v0.13.png` was rendered from checkpoint `208e8d9` before Kenney source
  removal.
- `after-v0.14a.png` uses the deterministic density fixture and Mini Medieval at
  the selected 3× default.

The comparison is development documentation, not a retained production art
pipeline.
