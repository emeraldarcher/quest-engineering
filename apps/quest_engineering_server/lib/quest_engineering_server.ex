defmodule QuestEngineering.Server do
  @moduledoc """
  Infrastructure boundary for the Quest Engineering control plane.

  Domain decisions belong in `QuestEngineering.Core`; this application owns HTTP,
  persistence, and runtime supervision.
  """
end
